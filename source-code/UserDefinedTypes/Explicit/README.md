# Explicit or not

Constructors with a single argument can lead to implicit conversions, which can
cause unexpected behavior. To prevent this, you can use the `explicit` keyword
in C++.


## What is it?

1. `explicit_or_not.cpp`: illustrates the difference between an explicit and an
   implicit constructor.
1. `CMakeLists.txt`: CMake configuration file to build the applications.
